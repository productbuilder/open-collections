# Open Collections Presenter (Scaffold)

`collection-presenter` is a minimal scaffold for future presentation-focused experiences.

It currently uses the shared app foundation primitives from `src/shared/ui/app-foundation/` so layout and placeholder styling are consistent with `app-shell` and `collection-account`.
