# items-model-v1 (top-level deployment test)

This folder is a top-level deployment test copy of the sandbox presentation prototype.

## Purpose

- Verify the app can be served directly from `src/apps/items-model-v1/` on the public site.
- Preserve behavior and structure from the sandbox prototype for path/deployment testing.

## Source of truth

The original prototype remains in:

- `src/apps/sandbox/items-model-v1/`

This top-level copy is intentionally kept functionally identical, except for minimal path/location-safe adjustments and a visible test label.
